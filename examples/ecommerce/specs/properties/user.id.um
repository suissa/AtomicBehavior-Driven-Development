property User.id {
  canonical_label "user.id"
  semantic_type EntityId
  primitive_envelope UUID

  required true

  // no normalizer declared
  validator GenerateEntityId
  validator RefuteEmptyId

  refutation EmptyId

  event UserId.success

  test_oracle UserIdTestOracle
}
