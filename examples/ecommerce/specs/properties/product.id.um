property Product.id {
  canonical_label "product.id"
  semantic_type EntityId
  primitive_envelope UUID

  required true

  // no normalizer declared
  validator GenerateEntityId
  validator RefuteEmptyId

  refutation EmptyId

  event ProductId.success

  test_oracle ProductIdTestOracle
}
