property Order.id {
  canonical_label "order.id"
  semantic_type EntityId
  primitive_envelope UUID

  required true

  // no normalizer declared
  validator GenerateEntityId
  validator RefuteEmptyId

  refutation EmptyId

  event OrderId.success

  test_oracle OrderIdTestOracle
}
